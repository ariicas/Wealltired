package com.class18;

public class ClassReview {

	
	
	
	
	
	
	
	
	
	/*
	 * Access Modifiers: 
	 * Helps access control the access to class properties/atrttibes and methods/behavior.
	 * There are four access modifiers;
	 * private
	 * default
	 * public
	 * proctected.
	 */
     /* PUBLIC- 
      * If we write public keyford before any property or method that means it can be access anywhere in our project.
       */
	 /*DEFAULT-
	 * Cab only be accessed within the same package.
	 */
	/* PROCTECTED 
	 * can be accessed inside the child classes 
	 */
	/*PRIVATE:
	 * can only be accessed within the same class
	 */
	/*METHOD DECLARATION 
	 * Access modifie(optional)
	 * return type(not optional) 
	 * name of method(not optional)
	 * () parameters are optional but parantheseis are not optional 
	 * {} (not optional)
	 * 
	 */
}
